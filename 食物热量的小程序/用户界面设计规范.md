# 用户界面与交互设计规范

## 设计理念

智能识别食物热量小程序采用简洁、直观、高效的设计理念，注重用户体验和功能易用性，让用户能够快速、准确地完成食物识别和热量记录。

## 设计原则

### 1. 用户体验原则
- **简洁明了**：界面简洁，信息层次清晰
- **操作便捷**：最少的操作步骤完成核心功能
- **反馈及时**：操作结果及时反馈给用户
- **容错性强**：提供错误处理和恢复机制

### 2. 视觉设计原则
- **一致性**：保持视觉风格和交互模式一致
- **可读性**：文字清晰易读，色彩对比适中
- **美观性**：符合现代审美，提升用户愉悦感
- **适配性**：适配不同屏幕尺寸和设备

## 整体架构

### 1. 页面结构设计

#### 1.1 主要页面架构
```
智能食物识别小程序
├── 首页 (Home)
│   ├── 快速识别入口
│   ├── 今日饮食概览
│   ├── 目标进度展示
│   └── 快捷功能入口
├── 识别页面 (Recognition)
│   ├── 拍照识别
│   ├── 相册选择
│   ├── 识别结果展示
│   └── 结果确认编辑
├── 记录页面 (Records)
│   ├── 饮食记录列表
│   ├── 添加记录
│   ├── 记录详情
│   └── 记录编辑
├── 分析页面 (Analytics)
│   ├── 热量统计图表
│   ├── 营养分析
│   ├── 趋势分析
│   └── 目标对比
├── 个人中心 (Profile)
│   ├── 用户信息
│   ├── 目标设置
│   ├── 设置选项
│   └── 帮助支持
└── 社区页面 (Community)
    ├── 动态分享
    ├── 健康挑战
    ├── 经验交流
    └── 排行榜
```

#### 1.2 导航设计
```javascript
// 底部导航配置
const tabBarConfig = {
  list: [
    {
      pagePath: 'pages/home/index',
      text: '首页',
      iconPath: 'images/home.png',
      selectedIconPath: 'images/home-active.png'
    },
    {
      pagePath: 'pages/recognition/index',
      text: '识别',
      iconPath: 'images/camera.png',
      selectedIconPath: 'images/camera-active.png'
    },
    {
      pagePath: 'pages/records/index',
      text: '记录',
      iconPath: 'images/records.png',
      selectedIconPath: 'images/records-active.png'
    },
    {
      pagePath: 'pages/analytics/index',
      text: '分析',
      iconPath: 'images/chart.png',
      selectedIconPath: 'images/chart-active.png'
    },
    {
      pagePath: 'pages/profile/index',
      text: '我的',
      iconPath: 'images/profile.png',
      selectedIconPath: 'images/profile-active.png'
    }
  ],
  color: '#999999',
  selectedColor: '#FF6B35',
  backgroundColor: '#FFFFFF',
  borderStyle: 'white'
};
```

### 2. 视觉设计规范

#### 2.1 色彩系统
```css
/* 主色调 */
:root {
  /* 主品牌色 */
  --primary-color: #FF6B35;        /* 活力橙 */
  --primary-light: #FF8A65;        /* 浅橙色 */
  --primary-dark: #E64A19;         /* 深橙色 */
  
  /* 辅助色 */
  --secondary-color: #4CAF50;      /* 成功绿 */
  --warning-color: #FF9800;        /* 警告橙 */
  --error-color: #F44336;          /* 错误红 */
  --info-color: #2196F3;           /* 信息蓝 */
  
  /* 中性色 */
  --text-primary: #212121;         /* 主要文字 */
  --text-secondary: #757575;       /* 次要文字 */
  --text-disabled: #BDBDBD;        /* 禁用文字 */
  --divider-color: #E0E0E0;        /* 分割线 */
  --background-color: #FAFAFA;     /* 背景色 */
  --surface-color: #FFFFFF;        /* 表面色 */
  
  /* 功能色 */
  --calorie-color: #FF6B35;        /* 热量相关 */
  --protein-color: #4CAF50;        /* 蛋白质相关 */
  --fat-color: #FF9800;            /* 脂肪相关 */
  --carbs-color: #2196F3;          /* 碳水化合物相关 */
}
```

#### 2.2 字体规范
```css
/* 字体系统 */
:root {
  /* 字体大小 */
  --font-size-xs: 10px;            /* 极小文字 */
  --font-size-sm: 12px;            /* 小号文字 */
  --font-size-base: 14px;          /* 基础文字 */
  --font-size-md: 16px;            /* 中等文字 */
  --font-size-lg: 18px;            /* 大号文字 */
  --font-size-xl: 20px;            /* 超大文字 */
  --font-size-xxl: 24px;           /* 标题文字 */
  
  /* 字重 */
  --font-weight-light: 300;
  --font-weight-normal: 400;
  --font-weight-medium: 500;
  --font-weight-bold: 600;
  
  /* 行高 */
  --line-height-tight: 1.2;
  --line-height-normal: 1.4;
  --line-height-relaxed: 1.6;
}
```

#### 2.3 间距系统
```css
/* 间距规范 */
:root {
  --spacing-xs: 4px;               /* 极小间距 */
  --spacing-sm: 8px;               /* 小间距 */
  --spacing-md: 16px;              /* 中等间距 */
  --spacing-lg: 24px;              /* 大间距 */
  --spacing-xl: 32px;              /* 超大间距 */
  --spacing-xxl: 48px;             /* 极大间距 */
  
  /* 组件间距 */
  --component-padding: 16px;
  --section-margin: 24px;
  --card-padding: 16px;
  --button-padding: 12px 24px;
}
```

### 3. 组件设计规范

#### 3.1 按钮组件
```css
/* 按钮样式规范 */
.btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  border: none;
  border-radius: 8px;
  font-size: var(--font-size-base);
  font-weight: var(--font-weight-medium);
  cursor: pointer;
  transition: all 0.2s ease;
  min-height: 44px; /* 最小触摸目标 */
}

/* 主要按钮 */
.btn-primary {
  background-color: var(--primary-color);
  color: white;
}

.btn-primary:hover {
  background-color: var(--primary-dark);
}

/* 次要按钮 */
.btn-secondary {
  background-color: transparent;
  color: var(--primary-color);
  border: 1px solid var(--primary-color);
}

/* 按钮尺寸 */
.btn-small {
  padding: 8px 16px;
  font-size: var(--font-size-sm);
  min-height: 36px;
}

.btn-large {
  padding: 16px 32px;
  font-size: var(--font-size-lg);
  min-height: 52px;
}
```

#### 3.2 卡片组件
```css
/* 卡片样式规范 */
.card {
  background-color: var(--surface-color);
  border-radius: 12px;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
  padding: var(--card-padding);
  margin-bottom: var(--spacing-md);
}

.card-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: var(--spacing-md);
  padding-bottom: var(--spacing-sm);
  border-bottom: 1px solid var(--divider-color);
}

.card-title {
  font-size: var(--font-size-lg);
  font-weight: var(--font-weight-medium);
  color: var(--text-primary);
}

.card-content {
  color: var(--text-secondary);
  line-height: var(--line-height-normal);
}
```

#### 3.3 输入组件
```css
/* 输入框样式 */
.input {
  width: 100%;
  padding: 12px 16px;
  border: 1px solid var(--divider-color);
  border-radius: 8px;
  font-size: var(--font-size-base);
  background-color: var(--surface-color);
  transition: border-color 0.2s ease;
}

.input:focus {
  outline: none;
  border-color: var(--primary-color);
  box-shadow: 0 0 0 2px rgba(255, 107, 53, 0.2);
}

.input-error {
  border-color: var(--error-color);
}

.input-disabled {
  background-color: var(--background-color);
  color: var(--text-disabled);
  cursor: not-allowed;
}
```

### 4. 页面设计详解

#### 4.1 首页设计
```html
<!-- 首页布局结构 -->
<view class="home-page">
  <!-- 顶部状态栏 -->
  <view class="status-bar">
    <view class="greeting">早上好，{{userName}}</view>
    <view class="date">{{currentDate}}</view>
  </view>
  
  <!-- 快速识别区域 -->
  <view class="quick-recognition">
    <view class="camera-button" bindtap="openCamera">
      <image src="/images/camera-icon.png" class="camera-icon"></image>
      <text class="camera-text">拍照识别食物</text>
    </view>
  </view>
  
  <!-- 今日概览 -->
  <view class="today-overview">
    <view class="overview-title">今日饮食</view>
    <view class="calorie-progress">
      <view class="progress-bar">
        <view class="progress-fill" style="width: {{calorieProgress}}%"></view>
      </view>
      <view class="calorie-text">{{todayCalories}} / {{targetCalories}} 卡路里</view>
    </view>
    <view class="nutrition-summary">
      <view class="nutrition-item">
        <text class="nutrition-label">蛋白质</text>
        <text class="nutrition-value">{{protein}}g</text>
      </view>
      <view class="nutrition-item">
        <text class="nutrition-label">脂肪</text>
        <text class="nutrition-value">{{fat}}g</text>
      </view>
      <view class="nutrition-item">
        <text class="nutrition-label">碳水</text>
        <text class="nutrition-value">{{carbs}}g</text>
      </view>
    </view>
  </view>
  
  <!-- 快捷功能 -->
  <view class="quick-actions">
    <view class="action-item" bindtap="addManualRecord">
      <image src="/images/add-icon.png" class="action-icon"></image>
      <text class="action-text">手动添加</text>
    </view>
    <view class="action-item" bindtap="viewHistory">
      <image src="/images/history-icon.png" class="action-icon"></image>
      <text class="action-text">历史记录</text>
    </view>
    <view class="action-item" bindtap="viewAnalytics">
      <image src="/images/chart-icon.png" class="action-icon"></image>
      <text class="action-text">数据分析</text>
    </view>
  </view>
</view>
```

#### 4.2 识别页面设计
```html
<!-- 识别页面布局 -->
<view class="recognition-page">
  <!-- 相机预览区域 -->
  <view class="camera-container">
    <camera 
      class="camera-preview"
      device-position="back"
      flash="off"
      bindscancode="onScanCode"
      binderror="onCameraError">
    </camera>
    
    <!-- 识别框 -->
    <view class="recognition-frame">
      <view class="frame-corner top-left"></view>
      <view class="frame-corner top-right"></view>
      <view class="frame-corner bottom-left"></view>
      <view class="frame-corner bottom-right"></view>
    </view>
    
    <!-- 提示文字 -->
    <view class="recognition-hint">
      <text>将食物放在识别框内</text>
    </view>
  </view>
  
  <!-- 底部操作栏 -->
  <view class="camera-actions">
    <view class="action-button" bindtap="switchCamera">
      <image src="/images/switch-camera.png" class="action-icon"></image>
    </view>
    <view class="capture-button" bindtap="capturePhoto">
      <view class="capture-inner"></view>
    </view>
    <view class="action-button" bindtap="selectFromAlbum">
      <image src="/images/album.png" class="action-icon"></image>
    </view>
  </view>
</view>
```

#### 4.3 识别结果页面
```html
<!-- 识别结果展示 -->
<view class="result-page">
  <!-- 识别图片 -->
  <view class="result-image">
    <image src="{{capturedImage}}" class="food-image"></image>
    <view class="confidence-badge">
      <text>识别准确度: {{confidence}}%</text>
    </view>
  </view>
  
  <!-- 识别结果 -->
  <view class="recognition-result">
    <view class="food-name">{{foodName}}</view>
    <view class="food-category">{{foodCategory}}</view>
    
    <!-- 营养信息 -->
    <view class="nutrition-info">
      <view class="nutrition-row">
        <text class="nutrition-label">热量</text>
        <text class="nutrition-value">{{calories}} 卡路里</text>
      </view>
      <view class="nutrition-row">
        <text class="nutrition-label">重量</text>
        <view class="weight-input">
          <input 
            type="number" 
            value="{{estimatedWeight}}" 
            bindinput="onWeightChange"
            placeholder="估算重量">
          <text class="unit">克</text>
        </view>
      </view>
    </view>
    
    <!-- 详细营养 -->
    <view class="detailed-nutrition">
      <view class="nutrition-item">
        <text class="label">蛋白质</text>
        <text class="value">{{protein}}g</text>
      </view>
      <view class="nutrition-item">
        <text class="label">脂肪</text>
        <text class="value">{{fat}}g</text>
      </view>
      <view class="nutrition-item">
        <text class="label">碳水化合物</text>
        <text class="value">{{carbs}}g</text>
      </view>
    </view>
  </view>
  
  <!-- 操作按钮 -->
  <view class="result-actions">
    <button class="btn btn-secondary" bindtap="editResult">编辑</button>
    <button class="btn btn-primary" bindtap="confirmResult">确认添加</button>
  </view>
</view>
```

### 5. 交互设计规范

#### 5.1 手势交互
```javascript
// 手势交互配置
const gestureConfig = {
  // 滑动操作
  swipe: {
    threshold: 50,        // 滑动阈值
    velocity: 0.3,        // 滑动速度
    direction: ['left', 'right', 'up', 'down']
  },
  
  // 长按操作
  longPress: {
    duration: 500,        // 长按时间(ms)
    feedback: 'haptic'    // 触觉反馈
  },
  
  // 双击操作
  doubleTap: {
    interval: 300,        // 双击间隔(ms)
    action: 'zoom'        // 双击动作
  }
};
```

#### 5.2 动画效果
```css
/* 页面转场动画 */
.page-transition {
  transition: transform 0.3s ease-in-out;
}

.page-enter {
  transform: translateX(100%);
}

.page-enter-active {
  transform: translateX(0);
}

.page-exit {
  transform: translateX(0);
}

.page-exit-active {
  transform: translateX(-100%);
}

/* 组件动画 */
.fade-in {
  animation: fadeIn 0.3s ease-in-out;
}

@keyframes fadeIn {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

/* 加载动画 */
.loading-spinner {
  animation: spin 1s linear infinite;
}

@keyframes spin {
  from {
    transform: rotate(0deg);
  }
  to {
    transform: rotate(360deg);
  }
}
```

#### 5.3 反馈机制
```javascript
// 用户反馈系统
class FeedbackManager {
  // 成功反馈
  showSuccess(message) {
    wx.showToast({
      title: message,
      icon: 'success',
      duration: 2000
    });
  }
  
  // 错误反馈
  showError(message) {
    wx.showToast({
      title: message,
      icon: 'error',
      duration: 2000
    });
  }
  
  // 加载反馈
  showLoading(message = '加载中...') {
    wx.showLoading({
      title: message,
      mask: true
    });
  }
  
  // 隐藏加载
  hideLoading() {
    wx.hideLoading();
  }
  
  // 触觉反馈
  hapticFeedback(type = 'light') {
    wx.vibrateShort({
      type: type
    });
  }
}
```

### 6. 响应式设计

#### 6.1 屏幕适配
```css
/* 响应式断点 */
@media screen and (max-width: 320px) {
  /* 小屏手机 */
  .container {
    padding: 12px;
  }
  
  .btn {
    min-height: 40px;
    font-size: 14px;
  }
}

@media screen and (min-width: 375px) and (max-width: 414px) {
  /* 标准屏手机 */
  .container {
    padding: 16px;
  }
}

@media screen and (min-width: 768px) {
  /* 平板设备 */
  .container {
    max-width: 600px;
    margin: 0 auto;
  }
  
  .grid-2 {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 16px;
  }
}
```

#### 6.2 安全区域适配
```css
/* 安全区域适配 */
.safe-area-top {
  padding-top: constant(safe-area-inset-top);
  padding-top: env(safe-area-inset-top);
}

.safe-area-bottom {
  padding-bottom: constant(safe-area-inset-bottom);
  padding-bottom: env(safe-area-inset-bottom);
}

.safe-area-left {
  padding-left: constant(safe-area-inset-left);
  padding-left: env(safe-area-inset-left);
}

.safe-area-right {
  padding-right: constant(safe-area-inset-right);
  padding-right: env(safe-area-inset-right);
}
```

### 7. 无障碍设计

#### 7.1 可访问性支持
```html
<!-- 无障碍标签 -->
<button 
  class="btn btn-primary"
  aria-label="拍照识别食物"
  role="button">
  <image src="/images/camera.png" alt="相机图标"></image>
  <text>拍照识别</text>
</button>

<!-- 语义化标签 -->
<main role="main">
  <section aria-labelledby="today-overview">
    <h2 id="today-overview">今日饮食概览</h2>
    <!-- 内容 -->
  </section>
</main>
```

#### 7.2 键盘导航
```css
/* 键盘焦点样式 */
.focusable:focus {
  outline: 2px solid var(--primary-color);
  outline-offset: 2px;
}

/* 跳过链接 */
.skip-link {
  position: absolute;
  top: -40px;
  left: 6px;
  background: var(--primary-color);
  color: white;
  padding: 8px;
  text-decoration: none;
  z-index: 1000;
}

.skip-link:focus {
  top: 6px;
}
```

### 8. 性能优化

#### 8.1 图片优化
```javascript
// 图片懒加载
class LazyImageLoader {
  constructor() {
    this.observer = new IntersectionObserver(this.handleIntersection.bind(this));
  }
  
  observe(element) {
    this.observer.observe(element);
  }
  
  handleIntersection(entries) {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        const img = entry.target;
        img.src = img.dataset.src;
        img.classList.remove('lazy');
        this.observer.unobserve(img);
      }
    });
  }
}

// 图片压缩
function compressImage(file, quality = 0.8) {
  return new Promise((resolve) => {
    const canvas = document.createElement('canvas');
    const ctx = canvas.getContext('2d');
    const img = new Image();
    
    img.onload = () => {
      canvas.width = img.width;
      canvas.height = img.height;
      ctx.drawImage(img, 0, 0);
      
      canvas.toBlob(resolve, 'image/jpeg', quality);
    };
    
    img.src = URL.createObjectURL(file);
  });
}
```

#### 8.2 渲染优化
```css
/* 硬件加速 */
.hardware-accelerated {
  transform: translateZ(0);
  will-change: transform;
}

/* 避免重排重绘 */
.stable-layout {
  contain: layout style paint;
}

/* 虚拟滚动 */
.virtual-scroll {
  height: 400px;
  overflow-y: auto;
}

.virtual-item {
  position: absolute;
  width: 100%;
}
```

---

*本设计规范将根据用户反馈和设计趋势持续更新优化*
